// Methods in strings

/* в cтринге индексы тоже начинаются с 0 */
let str = 'Welcome to Almaty'

/* method split('') - разделяет по аргументу в массив */
let mat = str.split(' ')
/* method join('') - объеденяет массив с аргументом, default ',' */
let str2 = arr2.join(';')

// str.includes('string') checks is there 'string' in this element

// str.startsWith('string') return bool

// str.endsWith('string') return bool

// str.toLowerCase()

// str.toUpperCase()

// str.replace('whichOne', 'forWhich')

// str.substr(3) start index to the end
// str.substr(7, 3) start index and how many
// str.substr(-5) from the end how many